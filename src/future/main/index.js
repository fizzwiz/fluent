import {Each} from './core/Each.js';
import {What} from './core/What.js';
/**
 * This is the most important package of the library, containing the classes:
 * 
 * - {@link Each} 
 * - {@link What}
 * 
 * @module core
 */
export const core = {
    Each,
    What
}